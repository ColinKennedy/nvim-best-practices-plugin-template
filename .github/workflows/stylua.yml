name: StyLua

on:
  pull_request:
    branches:
    - main

jobs:
  stylua:
    runs-on: ubuntu-latest
    name: StyLua

    steps:
    - uses: actions/checkout@v4

    - name: test
      run: |
        make check_with_stylua
